{
  "devDependencies": {
    "@types/node": "20.11.5",
    "gts": "^5.3.0",
    "typescript": "~5.4.3"
  },
  "name": "pdfme_test",
  "description": "```bash\r npm i typescript --save-dev\r npx tsc --init\r ```",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx tsc -w ./index.ts",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pdfme/common": "^4.0.1",
    "@pdfme/generator": "^4.0.1"
  }
}
